name: Trigger auto deployment for container-app-slk45jaw2dyco

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-slk45jaw2dyco-AutoDeployTrigger-f0847760-4143-4c10-9303-fb53ca8bfeef.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPSLK45JAW2DYCO_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPSLK45JAW2DYCO_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPSLK45JAW2DYCO_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPSLK45JAW2DYCO_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPSLK45JAW2DYCO_REGISTRY_PASSWORD }}
          containerAppName: container-app-slk45jaw2dyco
          resourceGroup: vs9945
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


